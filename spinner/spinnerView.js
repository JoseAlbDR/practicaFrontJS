/**
 * Build Spinner Function
 *
 * This function generates the HTML for a loading spinner.
 *
 * @returns {string} - A string containing the HTML for the loading spinner.
 */
export const buildSpinner = () => {
  return `<div class="loading"></div>`;
};
